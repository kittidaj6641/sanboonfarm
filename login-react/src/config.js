// API Configuration
const config = {
  // Use environment variable if available, otherwise fall back to current domain
  API_BASE_URL: 'https://sanboonfarm-production.up.railway.app'
};

export default config;
