// API Configuration
const config = {
  // Use environment variable if available, otherwise fall back to current domain
  API_BASE_URL: 'https://sanboonfarm-production.up.railway.app/member/water-quality-sensor'
};

export default config;
